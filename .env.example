# Copy to .env for local runs (docker run --env-file .env ...). Never commit .env.
# Keys mirror AGENTS.md "환경변수 / 설정".

SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/grape
SPRING_DATASOURCE_USERNAME=grape
SPRING_DATASOURCE_PASSWORD=grape

JWT_SECRET=change-me-to-a-random-32-byte-min-secret
# JWT_ACCESS_TTL=1h
# JWT_REFRESH_TTL=30d

GOOGLE_OAUTH_CLIENT_ID=
KAKAO_REST_API_KEY=

# SERVER_PORT=8080
