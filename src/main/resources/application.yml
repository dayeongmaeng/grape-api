spring:
  application:
    name: grape-api

  # Local dev: load KEY=VALUE lines from a project-root .env when present — IntelliJ / `gradlew bootRun`
  # don't read .env on their own. `optional:` -> no effect in Docker/prod, where .env is absent
  # (.dockerignore) and real environment variables are injected. `[.properties]` parses the
  # extensionless file as a properties file.
  config:
    import: optional:file:./.env[.properties]

  autoconfigure:
    # We authenticate via a custom JWT filter; the default in-memory user (and its logged
    # "generated security password") is unused.
    exclude:
      - org.springframework.boot.security.autoconfigure.UserDetailsServiceAutoConfiguration

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/grape}
    username: ${SPRING_DATASOURCE_USERNAME:grape}
    password: ${SPRING_DATASOURCE_PASSWORD:grape}

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate      # Flyway owns the schema; Hibernate only validates the mapping
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
        type:
          preferred_instant_jdbc_type: TIMESTAMP_WITH_TIMEZONE   # map Instant -> timestamptz (matches Flyway DDL)

  flyway:
    enabled: true
    locations: classpath:db/migration

  web:
    locale: en           # fixed -> Bean Validation messages render in English regardless of Accept-Language
    locale-resolver: fixed

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true

# Application-specific config. Prod values come from the environment (see AGENTS.md "환경변수 / 설정").
app:
  jwt:
    secret: ${JWT_SECRET:dev-only-insecure-secret-please-override-32bytes-min}
    access-ttl: ${JWT_ACCESS_TTL:1h}
    refresh-ttl: ${JWT_REFRESH_TTL:30d}
  oauth:
    google:
      # idToken의 aud는 아래 세 클라이언트 ID 중 하나와 일치해야 한다(kkori-api와 동일).
      # "Web application" 유형 — 웹 로그인. 클라이언트의 EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID와 동일.
      client-id: ${GOOGLE_OAUTH_CLIENT_ID:}
      # iOS/Android 네이티브 유형 — 네이티브 auth-code+PKCE 흐름의 idToken aud는 웹이 아닌 네이티브 클라이언트다.
      # 클라이언트의 EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID / EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID와 동일. 선택.
      ios-client-id: ${GOOGLE_IOS_OAUTH_CLIENT_ID:}
      android-client-id: ${GOOGLE_ANDROID_OAUTH_CLIENT_ID:}
    kakao:
      # 웹 로그인(POST /api/auth/kakao/web)의 authorization code를 access token으로 교환할 때 사용.
      # 네이티브 로그인은 토큰을 직접 보내므로 이 키가 필요 없다.
      rest-api-key: ${KAKAO_REST_API_KEY:}
      # 카카오 앱에서 "client secret"을 활성화한 경우에만 필요(그 외에는 비워 둠).
      client-secret: ${KAKAO_CLIENT_SECRET:}
  cors:
    # Browser origins allowed to call /api/**. Comma-separated; override per environment.
    # Dev: Expo web dev server (Metro default port 8081). Native (iOS sim / Android emulator) is
    # not a browser and is not subject to CORS, so it needs no entry here.
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:8081,http://127.0.0.1:8081,https://grape.kkori.co.kr}
